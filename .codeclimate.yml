engines:
   duplication:
      enabled: true
      config:
         languages:
            - php
   fixme:
      enabled: true
   phpcodesniffer:
      enabled: true
   phpmd:
      enabled: true
ratings:
   paths:
      - "**.php"
exclude_paths:
   - config/
   - tests/
   - database/
   - src/Spiritix/LadaCache/Database/Connection #These files are copy-pasted from Laravel
